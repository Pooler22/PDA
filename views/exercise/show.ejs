<% _.each(exercises, function(exercise) { %>
  <% if(chapter.id == exercise.owner){%>
    Nazwa:
    <%= exercise.name %>
      <br>
      Treść:
      <%= exercise.content %>
        <br>
        Kolejność:
        <%= exercise.order %>
          <br>
          Zawartość konsoli:
          <%= exercise.codeconsole %>
            <br>
            Test:
            <%= exercise.test %>
              <hr>
              <% } %>
                <% }) %>
